name: Placement-CI-Workflow

entry_points:
  main:
    command: "python modelling.py --n_estimators {n_estimators} --max_depth {max_depth}"
    parameters:
      n_estimators:
        type: int
        default: 100
      max_depth:
        type: int
        default: 5